{
  "name": "@monetizekit-examples/agent-ops",
  "private": true,
  "version": "0.0.0",
  "description": "AgentOps — a volume-based agent-style example: per-agent entities, pre-flight checks, dimensional usage metering, budget enforcement, and credit packs, all driven by MonetizeKit.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "next": "^15.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^7.0.2"
  }
}
